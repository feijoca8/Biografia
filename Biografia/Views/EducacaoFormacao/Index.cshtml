@model IEnumerable<Biografia.Models.Educacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Escolas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tempo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Escolas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tempo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EducacaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EducacaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EducacaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
